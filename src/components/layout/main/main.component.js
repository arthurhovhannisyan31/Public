// external libraries
import React from 'react'
// local services & data store
// local containers
// local components
// local constants
// local styles
import './main.style.scss'

const Main = ({children}) => {

  return (
    <div>
      <h1>Main</h1>
      {children}
    </div>
  )
}

export default Main

