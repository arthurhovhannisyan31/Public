// external libraries
import { all } from 'redux-saga/effects';
// local services & data store
// local containers
// local components
// local constants
// local styles

export default function* rootSaga() {
  yield all([]);
}
